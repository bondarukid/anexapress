import type { LucideIcon } from "lucide-react";

export type Feature299Pillar = {
  title: string;
  icon: LucideIcon;
};

export type Feature299SafariConfig = {
  mode: "default" | "simple";
  imageSrc: string;
};
